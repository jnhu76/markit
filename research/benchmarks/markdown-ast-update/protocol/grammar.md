# grammar.md — AUTHORITY POINTER (implementation deferred)

The BENCH-GRAMMAR-v1 semantic contract is FROZEN in
`R0-METHODOLOGY.md` §5 and is not restated or modified here.

Implementation materialization (shared scanner/grammar primitives, node
vocabulary) is deferred to R3 (grammar/corpus/mutation freeze) and R4 (H0
reference parser). R1 deliberately defines no Markdown node, AST/CST, tree,
fragment, or restart representation.
