# MQ-MATRIX — MARKIT-31 primary analysis (MQ1-MQ7)

| MQ | Evidence | Result | Qualification | Residual |
| -- | -------- | ------ | ------------- | -------- |
| MQ1 do incremental horses beat H0 end-to-end on the primary real workload? | case-weighted.csv, project-macro.csv (T_total, pairing preserved) | Yes on T_total for H2 1.368 / H3 1.447 / H4 2.342 (PROJECT_MACRO); NO for H1 1.059 macro-level (case geomean 0.989, median 0.690); CLEAN_STATE: all < 1.0 | LATENCY (QUALIFIED) | H1's macro parity hides project bimodality — see O6 |
| MQ2 does avoided work explain wins? | attribution-joined.csv: H0 inspects whole file (PA ~2030 median) vs H4 129 / H2 170 / H3 215; win magnitude orders with inspection reduction; E5/E1 sanity slot P9 (271B vs 5849B) | Yes — wins correlate with avoided source inspection; exceptions (E6, depth>=3) have their own work signatures | WORK_COUNTERS (QUALIFIED) + PARSE_AMPLIFICATION (QUALIFIED) | attribution is one observation per case x horse (not timed); consistency across 3 horses supports the mechanism |
| MQ3 fallback frequency and cumulative work | attribution-by-horse-family.csv: H1 fallback mean 0.646/case (E6/ATX 1.0, E4 0.795); fallback counter exists ONLY for H1 (schema); H4 restart median 111B / convergence 272B (E6: 3225B) | H1 falls back on 65% of real cases; H4 restarts are short except E4/E6 | WORK_COUNTERS (QUALIFIED); `fallback_to_full_count` is H1-schema only — absence for H2/H3/H4 means NOT_APPLICABLE, not zero | cross-horse fallback comparison impossible by schema (recorded) |
| MQ4 do structural regimes flip ranking or expose cliffs? | regime-container-depth.csv, regime-filesize.csv, regime-e6-reference.csv, regime-e4-convergence.csv | Yes: depth>=3 flips H2/H3/H4 below H0; <2KiB flips all below H0; E6 below H0 everywhere; H4 convergence cliff; H1 fallback cliff | LATENCY (QUALIFIED) + WORK_COUNTERS (QUALIFIED) | thresholds are descriptive bins; exact knee points NEEDS_CONTROLLED |
| MQ5 are source-inspection locality claims credible? | unique coverage vs cumulative effort reported SEPARATELY (§16/§17): unique_source_bytes vs source_bytes_inspected_total; PA vs inspection-effort-amplification in attribution-joined.csv / attribution-by-horse-family.csv | H4 unique 281B median but inspects 523B cumulative (1.9x re-visit); H2 380/888; H3 475/1107; H0 3431/6066. Locality claims hold directionally; re-visitation is 1.9-2.6x | WORK_COUNTERS (QUALIFIED), both families kept distinct | E6 slots show unique ≈ inspected ≈ whole file — locality truly lost there |
| MQ6 native-state cost vs oracle/verification cost? | Frozen contract: normalized projection, validation, checksum, oracle verification are OUTSIDE primary timing (result_row_v2 measurement covers prepare+native only; T_total==T_prepare+T_native verified on all 230400 timing rows) | **Primary latency excludes verification by contract; verification cost is NOT a qualified primary metric** (allowed answer, task §27 MQ6) | — (out of contract) | oracle/walk symbols appear in replay whole-process profiles only (NON_PRIMARY); a dedicated question if ever needed |
| MQ7 which metric families are qualified? | frozen answer set (task §27) | LATENCY QUALIFIED; WORK_COUNTERS QUALIFIED; PARSE_AMPLIFICATION QUALIFIED; CPU_TIME/ALLOC_COUNT/ALLOC_BYTES/PEAK_MEMORY/RETAINED_MEMORY UNAVAILABLE | — | perf-derived IPC/branch-miss ratios and allocator counts from Stage B are PROFILE_ONLY, never primary-qualified |
