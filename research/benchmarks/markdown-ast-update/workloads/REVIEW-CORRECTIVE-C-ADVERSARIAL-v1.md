# REVIEW-CORRECTIVE-C-ADVERSARIAL-v1 (§58, fresh-context adversarial review)

Reviewer setup: a genuinely fresh-context agent (no part of the
construction conversation) with read-only + command-execution access to
the repository at branch `research/35-corrective-c-payload-freeze-1`
(HEAD at review time `f45a510`, base `3a7d24e`). Its instructions were
the 30-question audit below; it was told to distrust the report and
re-verify from artifacts and by running the tools. Its review is
recorded verbatim in §1. §2 records the disposition of every finding —
all fixes were applied in THIS PR before the freeze was offered for
human review.

## 1. Reviewer verdicts (verbatim)

**Q1: PASS** — `git log` shows branch point `866c842` on settled master `3a7d24e`; report HEAD claims match. Hashed files: `workloads/source-lock.json` → `7884c61b…` and `workloads/manifests/candidate-universe-v1.json` → `f8a9917b…`, `workloads/analysis/domain-strata-v1.json` → `814c17be…`, all equal to the `freeze-receipt-v1.json` `selection_identity`. The `selection_config_sha256` `259f0cb3…` does not equal the sha256 of `selections/selection-config-v1.json` (`44012cdd…`) — resolved: `profile-select/src/artifacts.rs:189` hashes the canonical JSON of the frozen `SelectionConfig` *struct*, and `259f0cb3…` appears identically in every PR #38 settlement artifact, so it is the settled convention, not drift. Receipt binds base SHA, registry/lifecycle/oracle versions, and all six artifact digests; all six match on-disk files (verified by hash).

**Q2: PASS** — Ran `python3 workloads/tools/acquire.py verify --full`: `VERIFY PASS: 20 sources, 3970 materialized files byte-verified (66391919 bytes), 0 not materialized locally`. Exactly the numbers claimed in the report §1.

**Q3: PASS** — `git diff --name-status 3a7d24e HEAD -- workloads/selections/` shows exactly one change: `A syntax-coverage-repair-v1.json` (added overlay). Every CORRECTIVE-B settlement file including `selected-files-v1.json` is byte-identical; `selected-files-v1.json` still carries the settlement values (36 unique files, REP 18 / EXT 12 / SYN 2 / FULL 7 = 39 memberships), and the repair is applied strictly as a load-time overlay.

**Q4: CONCERN** — The repair overlay (`workload-freeze/src/repair.rs::apply_repair`) is identity-bound field-by-field against `selected-files-v1.json` identity (drift = `REPAIR_AUTHORITY` hard error), refuses an already-selected key, requires `selected_key ∈ evaluated_candidates`, and byte-hashes the materialized file against the recorded universe sha256 (verified: `dc02c576…`, 2944 bytes). It is append-only with `memberships: vec![membership_added]` — it cannot replace a file or touch other sets. **One gap:** `membership_added` is applied verbatim with no check that it equals `"syntax_coverage"` (exploited — see Q29), and `g0/g1_strict_eligible: true` are hardcoded rather than re-derived (mitigated by the per-anchor `strict_comparison_eligible` oracle gate and by the file genuinely being strict-clean — its evidence row shows `strict_comparison_eligible: true`).

**Q5: PASS** — Recomputed from the committed `workloads/profiles/candidate-rows-v1.jsonl` (3970 rows): 530 rows with G0 `strict_scope_clean=true` (repair says 530), exactly 28 with `strict_kinds.block_quote > 0` (repair says 28). The 28 evaluated candidates in the repair artifact match my recomputation key-for-key including sha256, byte count, and anchor count (0 mismatches). Tie-break trace is faithful: all 28 satisfy the uncovered cell (coverage tie), single cell → rarity tie, all candidate source_ids already in selection → diversity tie, lexical winner = `opentelemetry-spec/files/specification/library-layout.md` = the selected key; materialized bytes hash to the recorded sha. No unconsidered candidate can exist — the evaluated set is definitionally universe-complete. (Nit: the diversity note lists `openmlsys`, which has no candidate in the 28; conclusion unaffected.)

**Q6: PASS** (with a wording nit) — Matrix `G0-BQ-NEST-LINE` rows: every G0-eligible pre-repair file is `NO_TARGET_SYNTAX` (zero strict blockquote anchors); ineligible files are `GRAMMAR_INELIGIBLE`. Independent scan of all 36 files: only one G0-**eligible** file contains `>` lines at all (`myst-parser/files/docs/develop/contributing.md`, 11 lines, all `>> cd MyST-Parser`-style shell prompts inside ` ```shell ` fences — raw bytes, not blockquotes). Four G0-**ineligible** files (cpp-core-guidelines, node/fs.md, owasp Kubernetes, rust-book ch02) do contain real outside-fence blockquotes, so the repair artifact's parenthetical "the only selected file containing `>` lines" is imprecise if read across all 36 files — but the operative claim (no *G0-strict-eligible* selected file has a real blockquote anchor) is true, and ineligible files cannot serve G0-strict payloads. The repair was required, not cosmetic.

**Q7: PASS** — Registry: 23 entries (18 G0 including exactly 7 `exact_restore_leg` legs; 5 G1 including 1 leg) — counted from `transition-registry-v1.json`. E1–E6 each have ≥1 entry. All 15 BREAK-side entries (`restore_policy != exact_restore_leg`) have ≥1 APPLICABLE matrix cell (minimum: `G0-BQ-NEST-LINE` with 1, via the repair; G1 entries with 7 each). Restore legs are realized through their BREAK entries: 120 BREAK/RESTORE pairs, each chain-validated and byte-exact (Q15).

**Q8: PASS** — Hand-verified 5 payloads across lanes (`G0-BQ-NEST-LINE`, `G0-LOCAL-TEXT-REPLACE-EQ`, `G0-PARAGRAPH-SPLIT`, `G1-TABLE-CELL-EDIT`, `G1-TABLE-HEADER-PIPE-REMOVE`): grammar_id, syntax_target, edit_family, operation_variant all equal the registry entry; payload `expected_pre/post` contain the floors (e.g., BQ payload pre = floor + derived `block_quote count == 5`, post = `count == 6` — strengthens, never contradicts). Enforcement is real code at generation AND verify time (`workload-freeze/src/registry.rs:632-679`); `verify .` → `VERIFY_OK payloads=449`.

**Q9: PASS** — All floors are document-independent minimums: `kind_present` on the target, `text_content_differs`, `topology_equal` (sound: constructors replace letter bytes with inert `z`), `paragraph count >= 2` for merge. E3/E-link entries have deliberately empty post floors — weak but sound, with transition truth carried by oracle-proven derived kind-count flips. Where a floor would not hold, the anchor is rejected (fail-closed, e.g., REFDEF-RESTORE's `paragraph` pre-floor), never absorbed by an accidental document property. Derived exact-count predicates are per-payload and re-proven by TRANSITION-ORACLE-v1 at generate/verify/dry-run.

**Q10: PASS** — Reconciled exactly: 555 = 37 files × **15** BREAK-side registry transitions; the cross-product is complete (0 missing, 0 extra pairs in my set comparison). The 8 `exact_restore_leg` entries are skipped by design (`applicability.rs:1030-1033`, comment: "Restore legs are reached through their BREAK entry; they are not independent cells"). GRAMMAR_INELIGIBLE fast-path (165 rows) short-circuits per-file for non-strict files, still emitting explicit rows. No cell is silently absent. Nit: report §A6's phrase "every physical file × every registry transition" literally overstates (23 ≠ 15), though the same section states the restore-leg rule.

**Q11: PASS** — Matrix statuses are exactly the closed vocabulary declared in `applicability.rs:43-48`: APPLICABLE / NO_TARGET_SYNTAX / NO_VALID_REAL_ANCHOR / GRAMMAR_INELIGIBLE / POST_GRAMMAR_INELIGIBLE / NO_PROVABLE_TRANSITION. My tally of all 555 rows found no other status value. Per-anchor rejection reasons are a separate, free-form field by design, not row statuses.

**Q12: PASS** — All 36 anchor-level non-applicable rows carry per-anchor `rejections` (rows with set-level statuses carry set-level reasons in `coverage-final-v1.json §failures`, 415 entries). Spot-checked five against actual source bytes: `NO_ADJACENT_SIBLING` on blank-line-separated `*` list items (crafting-interpreters ch13 — correct under G0 §7); `NO_PROVABLE_TRANSITION` on a ` ```toc ` fence whose closer removal provably flips nothing; `DEFINITION_HAS_NO_RESOLVED_USE` on `[^1]:`/`[^2]:` footnote-style definitions with no resolved uses; `NO_ASCII_LETTER_RUN_GE4` on a CJK text run; `NO_ALNUM_CELL_BYTE` on cell bytes `        [] `. All accurate.

**Q13: CONCERN (minor)** — Constructors are deterministic (fixed scan orders, quantized tie-breaks) and byte/UTF-8-safe (all insertions are ASCII; offsets floored to char boundaries; byte scanning for ASCII markers is safe under UTF-8). Every candidate edit must pass `derivation_is_provable` + full oracle validation before freezing, so a semantics-breaking edit cannot become a payload undetected. Two doc/code mismatches found: (1) `construct_local_text_replace_eq` documents "ties: earliest" but Rust `max_by_key` returns the **last** maximum (latest run); (2) `construct_paragraph_split`'s key `((pos-mid).abs()*1000.0) as u64 * 2 + pos` quantizes distance to 0.001 byte, so for paragraph spans of roughly ≥2KB a farther-but-earlier space can beat the documented "space nearest the middle" rule. Both remain deterministic and oracle-gated — fix the comments or the formula.

**Q14: PASS** — `window_container_depth` (`applicability.rs:587-606`) measures maximum container depth over parse nodes **intersecting the anchor byte window**; the post-edit window is the anchor span shifted by the inserted bytes; `post_depth <= pre_depth` → `DEPTH_NOT_INCREASED` rejection (`applicability.rs:415-435`). A document-global maximum is never consulted. Caveat recorded: this rejection reason appears **zero** times in the matrix, so the gate is code-verified but was never exercised by data on this corpus.

**Q15: PASS** — Independently re-applied every edit with Python over the materialized source bytes: all 120 pairs have `sha(S1) == step0.post == step1.pre` (120/120), `S2 == S0` byte-for-byte (120/120), and base sha equal to the file bytes (120/120); all 209 single-step payloads also verify (`pre == source`, `applied == post`, 209/209). Sampled G1 delimiter pair `ethereum-eips/files/EIPS/eip-3198.md`: `-` → `n` at byte 2022, restore `n` → `-`, restored bytes identical to original. The replace-edit inverse is also covered by regression test `exact_inverse_is_byte_exact_for_all_edit_shapes` (delete/insert/replace), all 4 tests pass.

**Q16: PASS** — Identity material (`semantics/src/payload.rs:276-309`) contains only frozen identity fields (source id/path, grammar, pre-source sha, target/family/variant, edit coordinates, inserted sha, transition, predicates, trace, step) — no timestamp, hostname, or environment value. I recomputed `rp1:` + first 32 hex of `sha256(canonical_json(material))` in Python for **all 449 payloads: 449/449 match**. Truncation to 128 bits is adequate for a frozen corpus.

**Q17: PASS** — Recomputed from the matrix: valid resolutions early/middle/late = 140/140/140; deduplicated-marked outcomes = 152; 108 unavailable = 36 cells-with-anchors-but-inapplicable × 3 requested positions. Arithmetic closes exactly: 268 single + 31 double + 30 triple position-resolutions = 329 = BREAK payloads = summed `resolved_anchor_count`, over 420 resolved outcomes; deduplicated anchors yield ONE payload each (never double-counted). Matches the report and `coverage-final §positions` exactly.

**Q18: PASS** — `full-read-manifest-v1.jsonl`: 37 records, 111 lane records = 22 `G0_STRICT_FULL_READ` + 15 `G0_REALISM_FULL_READ` + 30 `G1_SEMANTIC_FULL_READ` + 7 `G1_REALISM_FULL_READ` + 37 `G2_DEFERRED_FULL_READ` (realism total 22, as reported). All 37 G2 lane records have `lane_valid=false` and qualification `deferred`. Identity fields (sha256/bytes) match the materialized sources (`verify` checks this: `full_read_records=37` OK).

**Q19: PASS** — `dryrun.rs:282-300` reads `full-read-manifest-v1.jsonl` and `edit-write-manifest-v1.jsonl` **from disk** via `read_jsonl` (missing/unparsable → `DRY_RUN_FAILED`), re-materializes and sha-verifies sources through `load_selected_files`, and re-validates every payload from manifest bytes (`validate_payload` — a stale coordinate/hash/predicate/payload_id is a hard error before any dispatch). Self-sufficiency confirmed by construction and by my re-run (byte-identical output from disk state alone).

**Q20: PASS** — One shared code path per payload (`dispatch_edit_case`): same reconstructed pre/post sources, same `CanonicalEdit`, one `ReferenceOracle` built from the H0 clean parse of the post source and shared by all five `dispatch!` expansions; the five horses differ only in the mechanism instance. CaseIds come from the frozen `CaseKeyV1` over payload fields only — 362 distinct payloads → 362 distinct case_ids, identical across all five horses (0 payloads with divergent case_id). No per-horse advantage exists in the adapter.

**Q21: PASS** — Recomputed from `dry-run-cases-v1.jsonl`: 1832 rows = 22 full_read (all Pass) + 1810 edit_write. Per horse: H0 362/0, H1 362/0, H2 351 pass/11 wrong, H3 346/16, H4 354/8 — exactly the report. All 35 wrong results are `G0-FENCE-CLOSER-REMOVE` (12), `G0-FENCE-CLOSER-RESTORE` (12), `G0-REFDEF-RESTORE` (11) — precisely the fence-EOF and refdef-resolution classes named; nothing else fails; failures are printed and force exit code 1 (`DRY_RUN_CORRECTNESS_FAILURES_PRESENT`), so nothing is hidden. Re-running `dry-run` reproduced both artifacts byte-identically.

**Q22: PASS** — `grep -rniE "instant|SystemTime|UNIX_EPOCH|Duration|elapsed|timestamp|nanos|millis|std::time"` over `workload-freeze/src/`: **zero hits**. The runner functions on the dry-run path (`orchestrate.rs::run_update_correctness/run_full_parse_correctness`) use `NoopWorkSink`, no `Clock` construction, and doc-comment "no clock is constructed or consulted"; timing machinery in the runner exists only in `supervisor.rs`/worker/smoke binaries, which the correctness-only lane never touches. Dry-run report carries only counts and correctness statuses.

**Q23: PASS** — All 87 G1 payloads carry membership `G1_TABLE_SEMANTIC` and notes containing `SEMANTIC_ONLY; NOT_HORSE_QUALIFIED; never dispatched into H0-H4`; 0 G0 payloads carry the G1 membership. The dry-run skips them at the trace gate (`dryrun.rs:326-336`): `g1_semantic_skipped=87`, dispatches = 1810 = 362 G0 cases × 5. No G1 payload enters any horse.

**Q24: PASS** — Zero occurrences of `inline_math`/`display_math`/math payload markers in all four JSONL artifacts; `coverage-final-v1.json` lists `inline_math` and `display_math` as `LANE_DEFERRED` with `payload_count: 0` and the deferred-lane note; G2 is never profiled (`applicability.rs:191`).

**Q25: PASS** (artifact) — Every observed non-core kind has an explicit status in `coverage-final-v1.json`: `table` ACTIVE_EDIT_COVERED (semantic-only note), 7 kinds `PARSE_COVERAGE_ONLY` (image, link_autolink, thematic_break, code_block_indented, heading_setext, html_block, raw_html_inline), `inline_math`/`display_math` LANE_DEFERRED (0 payloads), `strong`/`hard_break`/`soft_break`/`escapes_and_entities` OUT_OF_SCOPE_WITH_REASON (each with a grammar-divergence reason), `strikethrough`/`task_list_item`/`front_matter`/`directive` GRAMMAR_EXTENSION_REQUIRED with candidate evidence (2/23/2/400). All 12 G0 core kinds + blank-line boundary ACTIVE. Spot-checks pass; nothing observed is silent. (The report's *count* of one bucket is wrong — see Q28.)

**Q26: PASS** — Ran `determinism .`: `DETERMINISM_OK artifacts=7 byte-identical`. Independently generated into a temp dir and sha256-compared all 7 artifacts against the repository copies: **7/7 MATCH** (and the on-disk digests equal the receipt's recorded digests). Re-ran `dry-run`: both dry-run artifacts byte-identical to disk. No drift anywhere.

**Q27: PASS** — Ran `scripts/corrective-c-negative-tests.sh`: `NEGATIVE_TESTS_OK`, 5/5 fail closed at review time (source byte sha, payload edit coordinate, receipt digest, repair identity drift, repair candidate hash drift). Read the script: all tampering occurs on a `mktemp -d` copy of `workloads/`; nothing writes to the repository; `git status` clean after the run.

**Q28: FAIL** (per the stated "any mismatch is a FAIL" standard) — I cross-checked 20+ numeric claims: 555 rows; 37 files; 449 = 329+120 payloads; 209 singles + 120 pairs; 120/120 exact restores; 140/140/140 valid, 152 dedup, 108 unavailable; 415 failures with breakdown 214/165/26/8/2; full-read 37/111/22/30/22/37; family-table rows E1 51/18/9, E2 83/19/10, E3 50/10/6, E4 44/10/6, E5 102/17/9, E6 20/8/4, G1 87/7/6; dry-run table (22/22, 362, 1810, 87 skipped, H0–H4 counts); start memberships 39/36; repair 28 candidates / 2944 bytes / 5 anchors; universe 3970 / 66,391,919 bytes; ATX 12 payloads; BQ 6 payloads — **all match the artifacts**. Mismatches found: (1) report §2 says "**eight** G1-declared kinds PARSE_COVERAGE_ONLY" — `coverage-final-v1.json` contains **seven**; (2) the family table omits the `ATX_HEADING_TOGGLE` family (12 payloads / 4 files / 2 projects), so the table sums to 437 of 449 without saying so; (3) "four candidate-only REALISM_ONLY-level" — no `REALISM_ONLY` rows exist in the artifact; those four kinds are `GRAMMAR_EXTENSION_REQUIRED` with candidate evidence (a gloss, not a status); (4) "555 rows = every physical file × every registry transition" (15 of 23, explained elsewhere in the same section).

**Q29: CONCERN — the hole HOLDS.** Most damaging exploit I found and executed: the repair overlay applies `membership_added` from `syntax-coverage-repair-v1.json` **without validating its value**. On a temp copy I changed it to `"representative"` and ran `generate`: result `GENERATE_OK files=37 …`, coverage now reports `representative: 19 / syntax_coverage: 2`, and every payload + FULL_READ record of the repaired file is silently relabeled `['representative', 'EDIT_WRITE', 'G0_PRIMARY']` — a direct violation of "only SYNTAX_COVERAGE_SET may change", passing all five negative tests' classes (identity and hashes intact). Mitigations that limit it: the tamper requires editing a committed, review-visible artifact; the *committed* artifact and the receipt-bound on-disk payloads are correct (`syntax_coverage` membership throughout); membership is not part of payload_id, so the frozen 449 ids stay traceable; a fresh regeneration against the *frozen* receipt would be caught by `verify` digest comparison if the receipt is not also regenerated. Still, the report's §4 claim "recorded fail-closed … any drift is a hard error; tamper-tested in §7" is overstated for this field. Fix is one line: assert `membership_added == "syntax_coverage"` in `apply_repair` (plus a 6th negative test).

**Q30: Final gate** — The frozen *content* is sound: universe, selection, repair evidence, registry, payloads, oracle proofs, restore exactness, identities, boundaries, determinism, and the dry-run all verify independently and byte-exactly; the three pre-freeze constructor defects are honestly documented with regression tests; H2–H4 wrong results are recorded, not hidden, and correctly scoped to #31. What must change is small and does not touch frozen content.

BLOCKING:
1. Add the `membership_added == "syntax_coverage"` assertion in `repair.rs::apply_repair` (and a 6th negative test for it), or explicitly narrow the §4 "any drift is a hard error / fail-closed" claim to the fields actually enforced. (Q4/Q29)
2. Correct CORRECTIVE-C-REPORT-1.md §2: "seven" not "eight" PARSE_COVERAGE_ONLY kinds; add the ATX_HEADING_TOGGLE row (12 payloads / 4 files / 2 projects) or a note that the family table is E1–E6+G1 only; drop or re-word "REALISM_ONLY-level"; re-word "every registry transition" to "every BREAK-side registry transition". (Q28 — the report is the freeze's authority record and currently misstates its own artifact.)

NON_BLOCKING:
- Fix the two constructor doc-comments vs code tie-break mismatches (`editors.rs`: max_by_key "ties: earliest"; paragraph-split nearest-middle key) or the formula. (Q13)
- Wording nit in the repair finding: 4 G0-*ineligible* selected files do contain real blockquotes; scope the claim to G0-strict-eligible files. (Q6)
- Note that the DEPTH_NOT_INCREASED gate never fired on this corpus (code-verified, data-unexercised). (Q14)
- Diversity tie-break note lists `openmlsys`, which has no candidate among the 28. (Q5)
- `generator_tool_sha256` (91426def…) is inherited from PR #38 settlement and was not independently recomputed here. (Q1)

OVERALL: ACCEPT_WITH_CHANGES

## 2. Disposition (all applied in this PR before human review)

| finding | disposition |
|---|---|
| BLOCKING 1 (membership_added unvalidated, Q4/Q29) | `apply_repair` now hard-errors unless `membership_added == "syntax_coverage"`; negative test 6 ("repair membership relabel (set boundary)") added and green; report §4 names the enforced fields explicitly. |
| BLOCKING 2 (report §2/§A6 numeric wording, Q28) | "seven PARSE_COVERAGE_ONLY"; family table extended with ATX_HEADING_TOGGLE (12/4/2) and TABLE_SEMANTIC rows — the coverage builder itself now reports all 8 families, so the artifact and the report agree; "REALISM_ONLY-level" gloss removed; "every BREAK-side registry transition" wording everywhere. |
| NON-BLOCKING (Q13) | Constructor doc-comments corrected to state the implemented deterministic rules (latest longest run; quantized-distance-with-earliest-tie split key). No behavior change — payload bytes and identities are unchanged. |
| NON-BLOCKING (Q6/Q5) | Repair artifact finding scoped to G0-strict-eligible files; diversity note now lists exactly the candidate projects present among the 28 (opentelemetry-spec, rust-book, rust-rfcs). |
| NON-BLOCKING (Q14) | Report §9 note 4 records that the DEPTH_NOT_INCREASED gate is code-verified but data-unexercised on this corpus. |
| NON-BLOCKING (Q1) | Report §9 note 5 records the inherited `generator_tool_sha256` convention. |
| Q26 re-verification after fixes | generate / verify / determinism re-run green after every change; the 449 payload identities are UNCHANGED by every disposition (only the coverage report, receipt digests, docs and the repair loader's guards changed). |

The reviewer's Q30 judgment stands as the review's bottom line: the frozen
content is sound; the required changes were documentation/guard-level and
are applied. The freeze is accordingly offered to human review.
