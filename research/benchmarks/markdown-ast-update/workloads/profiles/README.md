# profiles/ — RESERVED

Reserved for later mechanism-neutral static characterization of the frozen
candidate snapshot under `../sources/` (#33 Stage A2). Per-file feature
vectors, project summaries, and the `ELIGIBILITY_BIAS_REPORT` belong here,
with a frozen profile schema version.

Acquisition must not populate structural parser profiles. This directory is
intentionally empty in round 1.
