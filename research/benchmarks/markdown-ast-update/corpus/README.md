# corpus/ — RESERVED for R3

Corpus generators, mutation definitions, real-world corpora, and
materialized payloads belong to R3 (BENCH-GRAMMAR-v1 + corpus/mutation
freeze). R1 uses only the tiny `R1_SMOKE_ONLY` fixture embedded in
`mechanisms/null-r1/src/fixture.rs`, which is NOT benchmark workload.
