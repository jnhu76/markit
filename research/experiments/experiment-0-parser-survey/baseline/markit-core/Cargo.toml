[package]
name = "markit-core"
description = "Experiment 0 measurement subject: pre-reset markit-core snapshot (no product role)"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# Intentionally empty (P0-01):
# - no GPUI and no other UI framework: core is framework-independent (ADR-002/008)
# - no platform/Windows crates: platform integration lives at the GPUI edge
# - no plugin runtime: only the semantic boundary is preserved
