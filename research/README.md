# Research

This directory separates **active benchmark research** from **closed historical experiments**.

```text
research/
  benchmarks/
    markdown-ast-update/      active H0-H4 benchmark + performance campaign

  experiments/
    ...                       closed / historical experiments; evidence only
```

## Active benchmark

`benchmarks/markdown-ast-update/` is the only active Markdown parser research
workspace.

Authority chain:

```text
#22 umbrella methodology
  -> R0-R5 frozen protocol / correctness substrate
  -> #31 project-driven performance evaluation
  -> Weakness Map
  -> future Markit-specific algorithm work
```

Do not promote material from `experiments/` into current authority without a
new evidence-backed decision.

For the active benchmark's detailed directory ownership and data lifecycle,
read:

- `benchmarks/markdown-ast-update/README.md`
- `benchmarks/markdown-ast-update/STRUCTURE.md`
