[workspace]
resolver = "2"
members = ["crates/markit-core", "crates/parser-survey"]

# EXPERIMENT-FIRST-RESET NOTE (#23 / MARKIT-EXPERIMENT-FIRST-REPO-RESET-1)
# ------------------------------------------------------------------------
# The only remaining members are the Experiment 0 parser-survey harness and
# the markit-core snapshot it measures. Both are historical research assets
# (issue #19 / PR #20), NOT product crates. They are moved out of this
# default workspace into research/experiments/experiment-0-parser-survey/
# by the next commit of that reset; this root workspace then dissolves.
# There is no active production Rust workspace on this branch.

[workspace.package]
version = "0.1.0"
edition = "2021"
