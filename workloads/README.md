# workloads/ — shared benchmark corpus & traces

Deterministic inputs shared by BOTH MVPs (GPUI and PocketJS), so a
measurement on one framework is the same work on the other. Phase A1
first-round corpus is ASCII/Latin only (PocketJS has no CJK system-font
discovery on main; CJK is a separate capability item).

## corpus/

| file | size | lines | content |
| --- | --- | --- | --- |
| `10k.txt` | ~10 KB | 189 | fixed-seed ASCII prose |
| `100k.txt` | ~100 KB | 1940 | same generator, same seed |
| `1m.txt` | ~1 MB | 19652 | same generator, same seed |

Generated by `generate-corpus.py` (fixed seed `0xA1C0FFEE`) — byte-identical
on every machine. Regenerate with:

```bash
python3 workloads/generate-corpus.py
```

## Planned (Phase B)

- `traces/` — framework-independent event traces
  (`{"type":"insert","text":"a"}, {"type":"key","key":"Backspace"}, ...`)
  replayed by both harnesses.
