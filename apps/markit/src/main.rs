//! Markit product application — skeleton.
//!
//! G0 (GPUI baseline selection) is not complete, so this binary has no UI
//! substrate yet and must not invent one: the window/editor shell lands
//! with the pinned GPUI baseline (roadmap G0 → P1). What exists here only
//! proves the product app crate consumes `markit-core` through the product
//! workspace.

fn main() {
    println!("Markit product app skeleton");
    println!("editor UI awaits the G0 GPUI baseline selection (docs/product/roadmap.md)");
}
